{
  "name": "todo-ai-chatbot-python-wrapper",
  "version": "1.0.0",
  "description": "AI-powered todo chatbot using OpenAI Agents SDK and MCP",
  "scripts": {
    "dev": "pip install -r requirements.txt && uvicorn api.main:app --reload --port 8000",
    "start": "pip install -r requirements.txt && uvicorn api.main:app --host 0.0.0.0 --port 8000",
    "test": "pip install -r requirements.txt && pytest tests/",
    "install": "pip install -r requirements.txt"
  },
  "keywords": ["ai", "chatbot", "todo", "openai", "mcp"],
  "author": "",
  "license": "MIT"
}